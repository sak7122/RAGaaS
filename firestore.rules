rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access goes through the Admin SDK backend.
    // Deny direct client access entirely to prevent cross-tenant data leaks.
    match /tenant_usage/{docId} {
      allow read, write: if false;
    }
  }
}
