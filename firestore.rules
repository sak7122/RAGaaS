rules_version = '2';

// All Firestore access goes through the backend service account (ADC / Cloud Run SA).
// No direct client-side Firestore access is permitted.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
